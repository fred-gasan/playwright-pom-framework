Test Case: Verify New “Application Security” Subdirectory is Created Under Learning Center in Sanity (Backend Only)

Title:
Verify creation and structure of the “Application Security” subdirectory under Learning Center in Sanity

Preconditions:

Access to Sanity Studio backend.

Learning Center root directory already exists.

Test Steps:

Open Sanity Studio and navigate to:
Generic Content Pages → Learning Center.

Validate that a new subdirectory named “Application Security” is present.

Open the “Application Security” directory and verify:

Folder loads without schema errors

Backend schema fields match the existing Learning Center subdirectory structure (AI, Application Delivery, Training).

Create a test child page under “Application Security” and check the generated slug/path.

Expected Result:

“Application Security” directory is successfully created under Learning Center.

Folder structure and schema match other Learning Center subdirectories.

Child page generates correct backend path in slug:
/learn/application-security/[child-page]

No validation or schema errors occur.



QA Comment

QA has validated that the new “Application Security” subdirectory is successfully created under Learning Center in Sanity. The folder loads correctly with no schema errors, matches the structure of other Learning Center subdirectories, and child pages generate the expected backend slug format:
/learn/application-security/[child-page].
Everything is working as expected.
